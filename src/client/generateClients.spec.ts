import generateClients from "./generateClients";

describe('generateClients', () => {
  it('should generate frontend and internal clients', () => {
    process.env.MICROSERVICE_NAMESPACE = 'default';
    // generateClients();
  })
})
