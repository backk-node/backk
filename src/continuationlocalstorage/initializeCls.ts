import { createNamespace } from 'cls-hooked';

export const multipleServiceFunctionExecutionsCls = createNamespace('multipleServiceFunctionExecutions');
export const serviceFunctionExecutionCls = createNamespace('serviceFunctionExecution');

export default function initializeCls() {
  // NO OPERATION
}
